# Server configuration
PORT=8080

# Database configuration (use DATABASE_URL for connection string or individual settings below)
DATABASE_URL=postgresql://user:password@host:port/dbname

# Alternative: Individual database connection parameters
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=postgres
DB_SSLMODE=disable
